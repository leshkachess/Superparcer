# Superparcer

Telegram Mini App для поиска одежды по бренду, размеру, цене, категории и магазинам.

## Быстрый запуск

Требуется Python 3.11+.

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -e ".[dev]"
python -m playwright install chromium
Copy-Item .env.example .env
uvicorn app.main:app --reload
```

Откройте `http://127.0.0.1:8000`. Подключены Mercari Japan, Grailed и Vinted.com.

Для запуска Telegram-бота заполните в `.env` новый `TELEGRAM_BOT_TOKEN` и публичный
HTTPS-адрес приложения в `MINI_APP_URL`, затем выполните:

```powershell
python -m app.bot
```

Локальный сервер нужно опубликовать по HTTPS (например, через Cloudflare Tunnel или
ngrok), поскольку Telegram Mini Apps не открывают обычный localhost.

## Добавление магазина

Создайте класс в `app/scrapers/`, унаследуйте его от `BaseScraper`, реализуйте асинхронный
метод `search()` и зарегистрируйте экземпляр в `app/scrapers/__init__.py`. Каждый магазин
нужно подключать с учётом его API, HTML-разметки, robots.txt и условий использования.

Mercari Japan загружает карточки динамически. Адаптер запускает Chromium, читает уже
отрисованный публичный каталог и не обращается напрямую к закрытым API. Он собирает
первую фотографию, название, цену и прямую ссылку. После сбора результаты строго
фильтруются по бренду и категории, японские названия переводятся на русский, а цены
пересчитываются из иен в доллары. Ценовой фильтр также принимает доллары. Каталог
разбит на страницы по 24 товара. Результаты кэшируются на 5 минут; лимит и время кэша
настраиваются через `.env`.

Grailed собирается с публичных страниц дизайнеров и категорий. Его цены уже указаны
в долларах; бренд сохраняется без перевода, а описание товара переводится на русский.

Vinted.com собирается только из публичного каталога: фото, название, размер, цена в
долларах и прямая ссылка. Бренд сохраняется без перевода, название переводится на
русский. Покупки и другие действия с аккаунтом Vinted приложение не выполняет.

Для обуви размер в фильтре задаётся в EU. Поиск автоматически принимает эквиваленты
в CM/JP и US, а также следующий полуразмер: например, EU 44 включает EU 44.5,
28/28.5 cm и US 10/10.5.

## Проверка

```powershell
ruff check .
pytest
```

## Railway

В корне проекта находятся `Dockerfile` с Chromium и `railway.toml` с healthcheck.
Railway автоматически передаёт приложению переменную `PORT`.

Для Telegram-бота задайте Railway-переменные `TELEGRAM_BOT_TOKEN` и `MINI_APP_URL`.
При старте приложение автоматически зарегистрирует защищённый webhook по адресу
`/telegram/webhook`; отдельный polling-процесс на Railway не требуется.
